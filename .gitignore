.DS_Store
.env.development.local
.env.local
.env.production.local
.env.test.local
.envrc
.eslintcache
.history
.idea
.vercel
.vscode
.yarn
*.log
*.tgz
build
dist
firebase
logs
node_modules
npm-debug.log*
package-lock.json
# the standalone server/ package uses npm — its lockfile is committed
!server/package-lock.json
yarn-debug.log*
yarn-error.log*
packages/excalidraw/types
coverage
dev-dist
html
meta*.json
.claude

# --- Personal fork: private data & secrets (never commit — see FORK_POLICY.md) ---
# Mounted drawings/metadata/thumbnails data dir (see DASHBOARD_PLAN.md)
/data/
.dashboard.json
.thumbnails/
# Local compose overrides & server-only config
docker-compose.override.yml
compose.override.yml
*.local.yml
*.local.yaml
# Secrets / tokens (Cloudflare tunnel token, GHCR PAT, Access service tokens, etc.)
*.token
.secrets
secrets/
